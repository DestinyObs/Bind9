$TTL    604800                  ; $TTL sets the default Time To Live for all records in this file (in seconds). 604800 = 7 days. This tells DNS resolvers how long to cache these records.

@       IN      SOA     ns1.services.cybacad.lab. admin.services.cybacad.lab. (
                        2024060201      ; Serial number for the zone. Increment this every time you change the file so secondaries know to update.
                        604800          ; Refresh interval: how often (in seconds) a secondary DNS checks for updates (7 days).
                        86400           ; Retry interval: if refresh fails, how long to wait before retrying (1 day).
                        2419200         ; Expire: how long before secondary DNS considers data invalid if it can't reach master (28 days).
                        604800 )        ; Negative Cache TTL: how long to cache negative responses (NXDOMAIN) (7 days).

@       IN      NS      ns1.services.cybacad.lab.        ; This zone's authoritative name server is ns1.services.cybacad.lab.

ns1     IN      A       172.16.40.2                       ; The DNS server for this services zone, with its static IP address (updated to .2).
dns     IN      A       172.16.40.2                       ; Alias for the DNS server (dns.services.cybacad.lab → ns1.services.cybacad.lab, updated to .2).

prometheus      IN      A       10.0.5.4                  ; Prometheus monitoring server for services.
grafana         IN      A       10.0.5.5                  ; Grafana dashboards server for services.
pulse           IN      A       10.0.5.8                  ; Pulse service node for services.
wazuh          IN      A        40.10.10.10               ; Wazuh security monitoring server for services.

metrics         IN      CNAME   prometheus                 ; Alias for Prometheus (metrics.services.cybacad.lab → prometheus.services.cybacad.lab).
monitor         IN      CNAME   grafana                    ; Alias for Grafana (monitor.services.cybacad.lab → grafana.services.cybacad.lab).

; Future services (ready for deployment, uncomment and update as needed)
; cicd           IN      A       <future-cicd-ip>         ; Placeholder for CI/CD service.
; db             IN      A       <future-db-ip>           ; Placeholder for database service.
